name: master

on:
  push:
    branches:
      - main # master 브랜치에서 push 이벤트가 일어났을 때 실행

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code.
        uses: actions/checkout@v3

      # - name: Install Dependencies
      #   run: npm install

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: aws-ssm-to-env
        uses: eliasjcjunior/aws-ssm-parameters-action@main
        with:
          recursive: true
          split-env: true
          upper-case: true
          paths: /dev/REACT_APP_CORS_URL

      - name: log envs
        env:
          REACT_APP_CORS_URL: ${{ env.REACT_APP_CORS_URL }}
        run: |
          echo ${{ env.REACT_APP_CORS_URL }}

      # - name: Build
      #   env:
      #     REACT_APP_CORS_URL: ${{ env.REACT_APP_CORS_URL }}
      #   run: |
      #     npm run build

      # - name: Debug show secret value
      #   run: |
      #     echo ${{ env.DEV_TEST }}

      # - name: Deploy to S3
      #   run: aws s3 sync build/ s3://${{ secrets.S3_BUCKET_ID }} --delete

      # - name: Invalidate CloudFront Cache
      #   run: aws cloudfront create-invalidation --distribution-id ${{ secrets.DISTRIBUTION_ID }} --paths "/*"
